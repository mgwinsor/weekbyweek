version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "database"
        out: "internal/database"
        emit_interface: true
        overrides:
          - column: "user.date_of_birth"
            go_type: "time.Time"
